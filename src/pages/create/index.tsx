  import * as React from 'react';
import Layout from '@/components/layout';
import { ArtCreateView } from '@/components/createSection';

export default function Artists() {
  return (
    <Layout>
      <ArtCreateView />
    </Layout>
  );
}
